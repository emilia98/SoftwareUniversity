function printNumbers(str) {
    let n = parseInt(str[0]);

    for(let cnt = n; cnt >= 1; cnt--){
        console.log(cnt);
    }
}

/* Uncomment to test:
printNumbers(["5"]);
printNumbers(["2"]);
*/