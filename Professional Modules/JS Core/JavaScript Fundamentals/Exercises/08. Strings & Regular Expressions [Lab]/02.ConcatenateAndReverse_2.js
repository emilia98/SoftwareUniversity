function concatAndReverse(array) {
   return Array.from(array.join('')).reverse().join('');
}

/* Uncomment to test:
console.log(concatAndReverse(['I', 'am', 'student']));
console.log(concatAndReverse(['race', 'car']));
*/
